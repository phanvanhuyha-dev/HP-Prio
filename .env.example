# === Vercel Postgres (tự động điền khi anh tạo Postgres DB trong dashboard Vercel > Storage) ===
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=

# === Google OAuth (Google Cloud Console > APIs & Services > Credentials) ===
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Email Google DUY NHẤT được phép đăng nhập vào app này
OWNER_EMAIL=ha.phan@vidu.com

# === NextAuth ===
# Random string bất kỳ, tạo bằng lệnh: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# === Gemini API (Google AI Studio: https://aistudio.google.com/app/apikey) ===
GEMINI_API_KEY=

# ĐỂ TRỐNG. App tự chọn model dùng được, không ghim phiên bản nào.
# Google liên tục ngừng cấp model cũ, ghim tên cụ thể là hỏng app sau vài tháng.
# Chỉ điền khi cần ép dùng đúng một model nào đó.
GEMINI_MODEL=

# === Web Push (tạo bằng lệnh: npx web-push generate-vapid-keys) ===
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=

# === Cron bảo mật (random string bất kỳ, tự đặt) ===
CRON_SECRET=

# === Lịch Outlook công ty qua Microsoft Graph (tùy chọn) ===
# Dùng khi công ty TẮT tính năng xuất bản lịch (Shared calendars) nên không
# lấy được liên kết ICS. Đăng ký ứng dụng tại https://entra.microsoft.com,
# quyền ủy nhiệm Calendars.Read và offline_access. Xem README mục "Lịch hôm nay".
# Không đặt ba biến này thì khu nối Microsoft tự ẩn, phần còn lại chạy bình thường.
MS_CLIENT_ID=
MS_CLIENT_SECRET=
# common: nhận mọi tài khoản. Điền mã tenant của công ty nếu muốn chỉ cho phép
# đúng tài khoản công ty đăng nhập.
MS_TENANT=common

# === Lịch họp bằng liên kết ICS (tùy chọn, và thường KHÔNG cần đặt) ===
# Lịch nay dán thẳng trong app: bấm nút tờ lịch ở góc trên bên phải. Liên kết
# lưu theo từng tài khoản nên không ai thấy lịch của ai.
#
# Biến dưới đây chỉ còn là đường lui cho bản cài từ trước: nó chỉ áp dụng cho
# tài khoản OWNER_EMAIL, và chỉ khi tài khoản đó chưa dán liên kết nào trong
# app. Nhiều lịch cách nhau bằng dấu phẩy. Xem README mục "Lịch hôm nay".
ICS_URLS=
